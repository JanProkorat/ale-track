services:
  postgres:
    image: postgres:17
    container_name: aletrack-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: AleTrack
    ports:
      - "5432:5432"
    volumes:
      - aletrack-data:/var/lib/postgresql/data

volumes:
  aletrack-data: