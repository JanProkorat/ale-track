# Copy to `.env` (git-ignored) and fill in real values.

# Backend REST API base URL (no trailing slash). The client appends the
# `ale-track/` route prefix itself.
VITE_API_BASE_URL=http://localhost:8080
