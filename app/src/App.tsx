// Placeholder shell — replaced in P1 (theme, providers, router, app shell).
export default function App() {
  return (
    <main style={{ display: 'grid', placeItems: 'center', minHeight: '100vh', fontFamily: 'system-ui' }}>
      <div style={{ textAlign: 'center' }}>
        <h1 style={{ margin: 0, letterSpacing: '-0.02em' }}>
          Ale<span style={{ color: '#F08C00' }}>Track</span>
        </h1>
        <p style={{ color: '#54606f' }}>Čistý React redesign — základ připraven (P0).</p>
      </div>
    </main>
  );
}
